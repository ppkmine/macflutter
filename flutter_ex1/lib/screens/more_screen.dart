//배터리 근데 조따 안다라 지긴 한다... ㅇ

import 'package:flutter/material.dart';

class MoreScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('More Text'),
    );
  }
}
